// MOVE [D-19] AND [D-18] FROM open TO resolved, WITH WHAT RESOLVED THEM.
//
//   node scratchpad/carried-p51-commit1.mjs
//
// An item leaves the carried register in exactly two ways: RESOLVED, with what resolved it, or
// WITHDRAWN, with why it was not a question after all. Both of these are resolved, and both
// resolutions carry a figure the item itself got wrong — which is the whole reason a prompt
// states what to ESTABLISH rather than what is true ([R-01]).

import { readFileSync, writeFileSync } from 'node:fs';

const PATH = 'adapters/pdf/maps/_carried.cross-form.json';
const doc = JSON.parse(readFileSync(PATH, 'utf8'));

const take = (id) => {
  const i = doc.open.findIndex((x) => x.id === id);
  if (i < 0) { console.error(`STOP — [${id}] is not in \`open\`. It cannot be resolved out of a list it is not in.`); process.exit(2); }
  return doc.open.splice(i, 1)[0];
};

const d19 = take('D-19');
const d18 = take('D-18');

d19.status = 'RESOLVED — Prompt 51 commit 1.';
d19.what_was_established_first = 'WHETHER A SECOND DEFINITION SOURCE ACTUALLY EXISTS, WHICH THE RULING SAID TO SETTLE BEFORE DECIDING. It does not, for any IRS form, and the reading is in two parts. (1) THE PORTAL: not one registry row for 433a, 433aoic, 433f or 433b is live — 334 rows, read against 1,289 live contact properties — while every row of every generated fields.<form>.json IS live (186 + 239 + 116 + 113 + 56 = 710, zero missing). (2) THE TREE: New-HubSpotProperties.ps1 already drops a registry form\'s rows when a generated file for that form exists, so those rows are not read by anything even when the portal is not consulted. A row that names nothing and that nothing reads is not a second source; it is a superseded sketch.';
d19.the_figure_the_item_got_wrong = 'SEVEN, AND IT IS 334. The item names seven rows for 433b because 433-B had just provisioned and somebody looked at that form. The 113 for 433a, 116 for 433aoic and 98 for 433f were in the identical state and had been for longer. Nothing in the tree could have said so, which is exactly what the new assertion changes.';
d19.the_second_finding = 'THE REGISTRY SPELLS 433-A(OIC) AS `433aoic` AND EVERY OTHER FILE SPELLS IT `433aoi`. New-HubSpotProperties.ps1 matches a generated file to a registry form by `meta.form` EQUALITY, so those 116 rows were never superseded by fields.433aoi.json and a provisioning run would have carried all 116 dead names forward. A supersession test that fails on a one-letter difference fails silently, which is why assert-registry-targets.mjs resolves a form key through a declared alias table rather than by equality, and exercises the alias in its canary.';
d19.the_third_finding = 'THE SAME DEFECT FACING THE OTHER WAY, ON TWO GENERATED FILES. gen-fields-from-map.mjs has always filtered its declared groups to the ones a row actually names — "so provisioning never creates an empty group in the CRM for a namespace nothing uses yet". The two DERIVERS declared their group list as a literal and never applied it, so fields.433b.json and fields.433boi.json each declared `irs433` and not one of their rows named it. Found by [RT-2]; both derivers now carry the same filter and both files were regenerated, the diff being exactly the four dropped lines and nothing else.';
d19.disposition = 'DELETED — 334 property rows across four forms, and one group row (`irs433a`, which is not even a live group on the portal). The three other group rows (irs433aoic, irs433f, irs433b) are live groups declared by their forms\' generated files, and they went because no row in THIS file claims them any more, not because they resolve to nothing.';
d19.what_survives_and_why_it_is_not_a_second_source = 'THE 48 `vlp` ROWS, ALL 48 LIVE. For the organizer routing properties the registry is the FIRST and ONLY definition source: there is no fields.vlp.json and no vlp map to generate one from, because those properties are not read off a printed page. Deleting them would delete the definitions themselves. The file now says this in its own meta rather than leaving a reader to infer it from what survived.';
d19.how_resolved = 'adapters/hubspot/assert-registry-targets.mjs — five separate conditions over every definition register in the tree, in both directions, with a canary that plants one violation of each and asserts a conforming register is still accepted. [RT-1] no generated rival, [RT-2] group declared (both ways: a row naming an undeclared group, and a group no row claims), [RT-3] form declared, [RT-4] every figure the register states about itself re-derives, [RT-5] the hs_name is live on the portal. [RT-1] through [RT-4] are OFFLINE and run in `npm run sweeps`; [RT-5] needs --portal and is UNASKED rather than passed when it is not run.';
d19.what_the_assertion_found_on_its_first_run = 'ACROSS ALL SIX REGISTERS, 758 ROWS. Before the surgery: 334 [RT-1] rows on fields.registry.json (reported as four form-level findings, one per form, each naming its row count and its generated rival) and 2 [RT-2] unclaimed group rows on the two derived files. After it: every row in all six resolves, on both tiers. The first DRAFT of the assertion also found something about itself — it asked [RT-1] of every register including generated ones, so each of the five generated files reported itself as its own rival, seven problems on a tree with two; and it read only `meta.forms`, which generated files spell `meta.form`, so it reported all 717 of their rows as naming an undeclared form. Both are recorded in the file at the line that fixes them.';
d19.the_offline_tier_is_the_stronger_half = 'A PORTAL READ ESTABLISHES WHAT IS TRUE NOW; [RT-1] ESTABLISHES THAT THE ROW CANNOT BE READ BY ANYTHING. It would have fired the day the first generated definitions file landed — three forms and many prompts before anybody read the portal against the registry — and it fires with no credential and no network.';

d18.status = 'RESOLVED — Prompt 51 commit 1. The class is swept; the fourth instance is repaired on all three forms that carry it.';
d18.the_count_that_was_derived = 'FORTY-FOUR pass-sensitive sites of 69 live-portal-state sites, across 15 per-form portal tools, derived by adapters/hubspot/post-pass-sweep.mjs from the tree on every run and printed there. The threshold this item set was TWO OR MORE, so the answer is BUILD and it is not close. The remaining 25 are 19 constructions (they build the live view and ask nothing of it) and 6 pass-invariant questions, each disposed by name.';
d18.the_fourth_instance = 'AND IT IS WHAT ACTUALLY SETTLED IT, because a count could have been argued about and this could not. derive-names-433boi.mjs asked "did THIS pass create this live property?" as `description.startsWith("433-B(OIC) (input key: K)")`. True on every run for the entire life of that form — until the 433-B pass, seven prompts later, had hs-describe-reused-433b.mjs rewrite nine shared descriptions to "Serves BOTH Form 433-B (input key: A) and Form 433-B(OIC) (input key: B)...". The predicate went false on four of them (the other five carry a backbone_key, which short-circuits A7 first) and `node adapters/hubspot/derive-names-433boi.mjs --portal` STOPPED. 433-B(OIC) could not regenerate its own definitions file, and it was found only because this cycle happened to need that regeneration.';
d18.why_the_fourth_is_a_worse_shape_than_the_three = 'THE THREE RECORDED INSTANCES ARE ALL SELF-INFLICTED — a tool broken by the pass IT precedes — and somebody hits those the next time they run the tool, which is soon. THE FOURTH WAS BROKEN BY ANOTHER FORM\'S PASS, and nobody re-runs a finished form\'s deriver. There is no natural moment at which that failure surfaces. That is the difference between a class three comments can hold and a class that needs a sweep.';
d18.the_repair = '`startsWith` becomes `includes` of the same token, on ALL THREE derivers in the same commit — 433-B(OIC) because it was broken, 433-B and 433-A(OIC) because they carry the identical predicate and are only unbroken by accident of evaluation order and of nothing having re-described their properties yet. A class repaired on one form and left on its neighbours is the reproduction [R-12] says to expect. The token stays disjoint in both directions and that is proved on five probes before anything is written: "(OIC)" sits between the form name and the paren, so "433-B (input key: K)" is not a substring of "433-B(OIC) (input key: K)" — unlike /433-B\\b/, which matches inside "433-B(OIC)" and IS the second instance this item records.';
d18.what_a_disposition_has_to_say = 'construction, pass-invariant, or pass-sensitive — and a pass-sensitive entry must name a `states` anchor, which the sweep asserts stands over a live line in EVERY file the entry covers. That is what "the tool knows which side of the pass it is on" means operationally, and it is the condition hs-dryrun-433b.mjs\'s repair already satisfies with its three-valued provisioningState. There is no fourth state: a site the shapes derive and the register does not name is a STOP.';
d18.how_resolved = 'adapters/hubspot/post-pass-sweep.mjs, wired into `npm run sweeps`, with its universe declared and printed, its five shapes printed, every catch disposed by name, and a canary that plants UNDISPOSED, ORPHAN and both shapes of UNSTATED against a synthetic file and asserts a conforming one raises nothing. Its PP ids are unique-checked against the other 51 registers by adapters/pdf/register-ids.mjs.';
d18.what_is_not_closed_by_it = 'The sweep asks whether a tool STATES which side of the pass it is on. It does not and cannot ask whether the statement is TRUE — [PP-15] would have been disposed as pass-sensitive-with-states on the day it broke, because `statusOf` was there and printing all along. What the sweep buys is that the population is enumerated and no member can be added silently; what caught the fourth instance was running the tool. Both are needed and only one of them is now automatic.';

doc.resolved.push(d19, d18);
doc._count = { open: doc.open.length, resolved: doc.resolved.length };
writeFileSync(PATH, JSON.stringify(doc, null, 1) + '\n');
console.log(`${PATH}: open ${doc.open.length}, resolved ${doc.resolved.length}`);
console.log(`  open ids: ${doc.open.map((x) => x.id).join(', ')}`);
